services:
  _defaults:
    autowire: true
    autoconfigure: true

  Domain\Business\Member\Repository\CreateMemberRepositoryInterface: '@Infrastructure\Repository\CreateMemberRepository'
  Domain\Utils\Identifier\IdentifierInterface: '@Application\Utils\Identifier\IdentifierV1'
  Application\:
    resource: "../../src/Application"
  Application\Business\Member\Command\Hanlder\CreateMemberHandler:
    tags: [{ name: messenger.message_handler, bus: command.bus }]
    # prevent handlers from being registered twice (or you can remove
    # the MessageHandlerInterface that autoconfigure uses to find handlers)
    autoconfigure: false
